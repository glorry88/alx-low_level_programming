#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints alphabets without q and e
 * Return: Always 0 (Success)
 */
int main(void)
{
for( int x = 0; x < 10; x++)
printf("%d", x);
 return (0);
}
