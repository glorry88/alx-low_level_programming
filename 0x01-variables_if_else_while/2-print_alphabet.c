#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints alphabet s without q and e
 * Return: Always 0 (Success)
 */
int main(void)
{
char alpha;
for (alpha = 97; alpha <= 122; alpha++)
putchar(alpha);
return (0);
}
