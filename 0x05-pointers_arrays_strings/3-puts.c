#include "holberton.h"
/**
 * _strlen - function to print to print a string
 * @s: string parameter
 * Return: string followed by new line
 */
void _puts(char *str);
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
_putchar(s[i]);
}
_putchar('\n');
}
